[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

# Disable the automatic Next.js plugin that causes issues
[[plugins]]
  package = "@netlify/plugin-nextjs"
  [plugins.inputs]
    disable = true
